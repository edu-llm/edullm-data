# The source registry — 17 corpora, and what is measured vs. assumed

`corpus-registry.json` is generated by `build_registry.py`; edit the script, never the JSON. Every row
loads into `edullm_data.corpus.CorpusSpec` as a build-time check, so a registry that would crash the
reader fails when it is generated.

**17 corpora · 14 drawn in v1 · 3 reserve · 252.6 B target.**

## Read this before using any number

**`text_column` is verified for 9 of 17.** The rest say the literal string `"UNVERIFIED"`, and each
carries the exact command that settles it in its first `traps` entry. That is not a formality: the
column is the one field where being wrong is *completely silent*.

**`pool_tokens` are dolma2 re-counts**, from `artifacts/recount/` and `artifacts/sizing-revised.md`,
never card figures. §3.1 is blunt about why: of ~35 corpora surveyed only ~10 name their tokenizer,
AI2 and NVIDIA name none, and every Common Pile "token" figure is `Size(GB) × 0.25` — pure arithmetic
dressed as a measurement. Where we have no measurement we trust, the field is `null`, which is honest
in a way a card figure would not be.

**`target_tokens: 0` means RESERVE** — specced, traps recorded, deliberately not drawn. Three rows:
`essential-web`, `arxiv_papers_filtered`, `github_archive_filtered`. They are listed *because* they
were considered; deleting them would lose the measurement and the reasoning.

## Pools vs. targets

| category | target | non-overlapping pool | ratio | |
|---|---|---|---|---|
| edu-web | 48.0 B | 261.3 B | 5.44× | |
| web (diverse) | 30.0 B | 114.7 B | 3.82× | |
| code | 40.0 B | 74.8 B | 1.87× | one source only |
| synthetic | 60.0 B | 478.1 B | 7.97× | |
| math | 34.0 B | **34.7 B** | **1.02×** | ⚠ overlap-adjusted, no slack |
| academic | 20.0 B | **64.1 B** | 3.21× | ⚠ overlap-adjusted (naive sum 84.2 B) |
| reference | 8.8 B | **8.9 B** | **1.01×** | ⚠ no slack |
| QA/forum | 11.8 B | 37.4 B | 3.17× | |

**Two categories are reported twice, and the difference matters.** Summing the rows overstates
`academic` by 20.1 B and `math` by everything that looks like a second source, so the JSON carries
`naive_sum_pool_tokens` *and* `non_overlapping_pool_tokens` side by side with a `_pool_note`. Both,
never one — the naive sum is what a reader will recompute from the rows and expect to match, and
silently substituting the adjusted figure would make the arithmetic unreproducible.

- **academic**: 49.7% of peS2o's *bytes* are PubMedCentral-derived, measured from per-document
  `metadata.pdf_src`. The same article extracted by Grobid-over-PDF and by pandoc-over-nXML differs
  in >10% of its 20-grams and **survives fuzzy dedup as distinct** — a digest never catches it and
  neither does MinHash at the usual threshold. Drop peS2o's PMC share rather than dedup it.
- **math**: exactly one independent source. `swallow-math-v2` (32 B, Apache-2.0) is a FineMath
  rewrite whose measured self-similarity is too low for dedup to catch, so adding it would inflate
  the number without adding content.

### ⚠️ The no-slack finding, and the mistake I nearly published from it

`math` at 1.02× and `reference` at 1.01× have essentially no headroom: the target consumes the pool.
**But decontamination is not the threat**, and my first estimate said it was — by four orders of
magnitude.

The only real attrition measurement available is `datamix1-jul22`'s `leakage-summary.json`, whose
`category_attrition` block reads like a pool fraction: `math 0.543`, `code 0.797`. Propagated as
such it says math loses 54% of 34.69 B and comes up 18 B short — alarming, and wrong. Those are
**excluded ÷ candidates within a category**: math had **3,926 candidate documents**, not 34.69 B
tokens. The whole 20 B build excluded **10,239 documents ≈ 0.026% of tokens.**

What these categories genuinely cannot absorb is a *sourcing* surprise — a corpus that re-counts
lower under dolma2, or a source dropped on license grounds. Neither is hypothetical: `reference` was
already resized 14 B → 9 B when `finewiki/en` measured 8.87 B, and QA/forum falls to 1.87 B if
share-alike is ever excluded.

## The traps worth reading before you build

**FinePhrase — the rewrite is at `rollout_results.list.element.text`.** Top-level `text` holds the
ORIGINAL FineWeb-Edu document; its `dataset` field literally reads `HuggingFaceFW/fineweb-edu`. A
flat leaf list contains `text` **twice** and `.names.index("text")` returns the original, so building
from it yields a corpus of unrephrased FineWeb-Edu labelled synthetic — and no hash, size, or decode
check catches that. Worse, `rollout_results.text` does not raise either: it returns a table with
**zero columns**. Verified through the finished reader; the correct selector returns the rewrite and
both near-miss spellings are rejected.

**The four FinePhrase formats are ~91–93% the same documents.** Apply the §9.7 item 4 partition
(`sha256(id) % 4 == f`) **before tokenizing** — afterwards there is no document→id mapping and it
cannot be retrofitted. Each format needs 10.1–17.3% of its pool; a disjoint quarter gives 25.0%
(measured 24.86–25.26%).

**FineWeb-Edu's overlap is total, not partial.** `sample-100BT ⊂ sample-350BT`, which is FinePhrase's
exact parent, so **100%** of an edu-web draw has a synthetic sibling. The free fix is to draw
synthetic from the ~242 M `sample-350BT` ids *not* in `sample-100BT`.

**Two sources ship a `domain` and both need slugging and folding.** `stackv2-edu` has 73 languages
via `metadata.gha_language`; `stackexchange` has ~180 sites via `metadata.site`. Cardinality is
permanent — every distinct value becomes a directory inside `manifest_sha256`. Fold to the top ~20,
rest → `other`, and remember the map is a **reader argument**, not a spec field, because a streaming
pass cannot know the top 20. Gate A now rejects a published segment containing `#` or brackets, but
everything else still needs `slug_path_segment`.

**Short documents are a publish blocker, not a quality preference.** `ubuntu-irc` is IRC turns and
FinePhrase has a sampled rewrite that is 12 tokens long. A mean under 20 tokens per document puts the
packed shard's EOS fraction over the family's 0.05 bound and Gate A rejects it — *after* the tokenize
and the upload. `corpus.MIN_DOC_TOKENS` is what prevents that.

**Licensing, recorded as strings and not booleans.** QA/forum is 92.8% share-alike and `finewiki`
carries CC-BY-SA **and** GFDL, two different copyleft regimes. `stackexchange`'s CC-BY-SA is visible
only in per-row `metadata.all_licenses` — its `cardData` declares nothing — while peS2o's ~1.9% SA is
invisible from repo metadata entirely. A declared license can both overstate and understate what is
inside. *Licensing notes are research findings, not legal advice.*

## What is not here

- **`.json.zst`** — `corpus_read` refuses it loudly; it needs a `zstandard` dependency this package
  does not declare. No row currently requires it.
- **`revision` shas** are `null` throughout. Pin them before the real build, or a re-download can
  silently return different bytes for the same "corpus."
- **`id_column`** is `UNVERIFIED` on every row but the FinePhrase four. It matters most where the id
  is a join key — the FinePhrase partition and the FineWeb-Edu anti-join.
- **Nemotron** is absent by decision, not omission: its license forbids making the data available to
  others, which a shared reservoir does by definition (§7 item 1).
